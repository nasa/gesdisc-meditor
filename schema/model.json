{
  "$schema": "http://json-schema.org/schema#",
  "id": "https://disc.gsfc.nasa.gov/schemas/model.json",
  "type": "object",
  "properties": {
    "titleProperty": {
      "$comment": "Property that should be used as a title field for documents belonging to the model",
      "title": "Title Property",
      "description": "Property that should be used as a title field for documents belonging to the model",
      "type": "string",
      "required": true
    },
    "name": {
      "$comment": "Name of the model (ex: FAQ, Alert, Collection etc.,): displayed to the user in UI",
      "title": "Name",
      "description": "Name of the model (ex: FAQ, Alert, Collection etc.,)",
      "type": "string",
      "minLength": 3,
      "required": true
    },
    "description": {
      "$comment": "Description of the model (ex: FAQ, Alert, Collection etc.,): might be shown to user as a tooltip",
      "title": "Description",
      "description": "Description of the model (ex: FAQ, Alert, Collection etc.,)",
      "type": "string",
      "minLength": 10,
      "required": true
    },
    "category": {
      "$comment": "Category to which the model belongs; used for grouping models in user interfaces",
      "title": "Category",
      "description": "Category to which the model belongs; used for grouping models in user interfaces",
      "type": "string",
      "required": false
    },
    "workflow": {
      "$comment": "Workflow to be used for publishing documents of the model",
      "title": "Workflow",
      "description": "Workflow to be used for publishing documents of the model",
      "type": "string",
      "required": "true",
      "default": "Default"
    },
    "icon": {
      "$comment": "Icon for the model: currently limited to Font Awesome",
      "title": "Icon",
      "description": "Icon for the model: currently limited to Font Awesome",
      "type": "object",
      "properties": {
        "name": {
          "$comment": "Name of the Font Awesome icon (ex: fa-warning)",
          "title": "Icon name",
          "description": "Name of the Font Awesome icon (ex: fa-warning)",
          "type": "string",
          "required": true
        },
        "color": {
          "$comment": "Background color to be used for the Font Awesome icon (ex: #000000)",
          "title": "Icon background color",
          "description": "Background color to be used for the Font Awesome icon (ex: #000000)",
          "type": "string",
          "required": false
        }
      }
    },
    "schema": {
      "$comment": "JJSON Schema representing the model",
      "title": "Schema",
      "description": "JSON Schema representing the model",
      "type": "string",
      "required": true
    },
    "layout": {
      "$comment": "Layout of the model for user interface",
      "title": "Layout",
      "description": "Layout of the model for user interface",
      "type": "string",
      "required": true
    },
    "documentation": {
      "$comment": "Link to model's documentation",
      "title": "Help URL",
      "description": "Link to model's documentation",
      "type": "string",
      "required": false
    },
    "tag": {
      "$comment": "An array of tags associated with the model: can be used for searching models",
      "title": "Tag",
      "description": "Tags associated with the model: can be used for searching/grouping models",
      "type": "array",
      "items": {
        "type": "string"
      },
      "required": false
    }
  }
}
